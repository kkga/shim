---
title: Get started
description: Setup the components in your project.
order: 1
---

Shim is a pre-styled collection of copy-paste UI components built on top of [React Aria Components](https://react-spectrum.adobe.com/react-aria/index.html). You own the code.

{/* <Note title="Warning" intent="warning" size={2}>
  This documentation is a work in progress. Things might not work as described.
</Note> */}

## Install the prerequisites

Shim components depend on several libraries. Use the commands below to install them.

<Step
  title="React Aria Components"
  code="pnpm add react-aria-components" 
  description={
    <p>
      Core unstyled primitives used by Shim components.<br/>
      Read the docs at <a href="https://react-spectrum.adobe.com/react-aria/components.html">react-spectrum.adobe.com/react-aria/components</a>.
    </p>
  }
/>

<Step
  title="Tailwind CSS v4"
  description={
    <p>
      Utility-first styling foundation.<br/>
      Installation depends on your framework—follow the <a href="https://tailwindcss.com/docs">official Tailwind guide</a> for the correct steps.
    </p>
  } 
/>

<Step
  title="tailwind-variants and tailwind-merge"
  code="pnpm add tailwind-variants tailwind-merge"
  description={
    <p>
      Declarative <a href="https://www.tailwind-variants.org/">variant helpers</a> and <a href="https://github.com/dcastil/tailwind-merge">merge utilities</a>.
    </p>
  }
/>

<Step
  title="Radix Colors"
  code="pnpm add @radix-ui/colors"
  description={
    <p>
      Semantic color palette used by Shim tokens.<br/>
      Browse palettes at <a href="https://www.radix-ui.com/colors">radix-ui.com/colors</a>.
    </p>
  }
/>

<Step
  title="Phosphor Icons"
  code="pnpm add @phosphor-icons/react" 
  description={
    <p>
      Icon set referenced by several components.<br/>
      Explore the catalog at <a href="https://phosphoricons.com/">phosphoricons.com</a>.
    </p>
  }
/>


## Set up Shim

The Shim CLI helps you set up your project and add components.

<Step
  reset
  title="Configure path alias"
  description={
    <p>
      Shim components import files via the <strong>@/</strong> alias.<br/>
      Configure the path in <strong>tsconfig.json</strong> (or <strong>jsconfig.json</strong>) so those imports resolve.
    </p>
  }
  code={[{
    title: "tsconfig.json",
    content: `{
    "compilerOptions": {
      "baseUrl": ".",
      "paths": {
        "@/*": ["./*"]
      }
    }
}`}]}
/>

<Step
  title="Initialize Shim"
  description={
    <p>
      Run the <code>init</code> command to initialize Shim.<br/>
      It creates a <strong>shim.config.json</strong> file in your project root that
      defines where components, utility files, and CSS are installed.
    </p>
  }
  code="pnpm dlx @kkga/shim init"
/>

<Step
  title="Add components"
  description={
    <p>
      Use the <code>add</code> command to copy components into your project.<br/>
      The CLI resolves dependencies, rewrites imports, and installs into the paths defined in <strong>shim.config.json</strong>.
    </p>
  }
  code="pnpm dlx @kkga/shim add button"
/>


<Note title="Todo" intent="warning" size={2}>
  Manual setup instructions.
</Note>

