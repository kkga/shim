---
title: Changelog
description: Recent updates and changes to Shim.
order: 4
---

## 27 November 2025

### Disclosure and DisclosureGroup

* Add `soft` and `ghost` [variants](/components/disclosure-group#disclosure-group-variant) (in addition to the existing `surface` variant);
* Simplify `Disclosure` anatomy by removing the `DisclosureHeader` and `DisclosurePanel` subcomponents, replaced with `title` and `children` props;

***

## 26 November 2025

### Table

* Add `ghost` and `zebra` [variants](/components/table#table-variant) (in addition to the existing `surface` variant);
* Add [selection example](/components/table#table-selection);
* Add support for `size` prop and [size example](/components/table#table-size);

### Tooltip

* Add support for `size` prop and [size example](/components/tooltip#tooltip-size);
