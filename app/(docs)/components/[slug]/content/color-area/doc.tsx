import type { DocModule } from "@/app/(docs)/components/schema";

export default {
  sections: [
    {
      demo: {
        group: "color-area",
        name: "main",
      },
    },
  ],
} satisfies DocModule;
