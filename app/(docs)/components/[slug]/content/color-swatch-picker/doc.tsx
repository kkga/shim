import type { DocModule } from "@/app/(docs)/components/schema";

export default {
  sections: [
    {
      demo: {
        group: "color-swatch-picker",
        name: "main",
      },
    },
  ],
} satisfies DocModule;
