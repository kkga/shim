import type { DocModule } from "@/app/(docs)/components/schema";

export default {
  sections: [
    {
      demo: {
        group: "color-field",
        name: "main",
      },
    },
  ],
} satisfies DocModule;
