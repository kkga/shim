import type { DocModule } from "@/app/(docs)/components/schema";

export default {
  sections: [
    {
      demo: {
        group: "drop-zone",
        name: "main",
      },
    },
  ],
} satisfies DocModule;
