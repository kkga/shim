import type { DocModule } from "@/app/(docs)/components/schema";

export default {
  sections: [
    {
      demo: {
        group: "tree",
        name: "main",
      },
    },
  ],
} satisfies DocModule;
