import type { DocModule } from "@/app/(docs)/components/schema";

export default {
  sections: [
    {
      demo: {
        group: "toast",
        name: "main",
      },
    },
  ],
} satisfies DocModule;
