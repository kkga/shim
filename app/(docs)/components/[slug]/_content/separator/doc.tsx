import type { DocModule } from "@/app/(docs)/components/schema";

export default {
  sections: [
    {
      demo: {
        group: "separator",
        name: "main",
      },
    },
  ],
} satisfies DocModule;
