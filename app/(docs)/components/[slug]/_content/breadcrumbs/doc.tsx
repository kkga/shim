import type { DocModule } from "@/app/(docs)/components/schema";

export default {
  sections: [
    {
      demo: {
        group: "breadcrumbs",
        name: "main",
      },
    },
  ],
} satisfies DocModule;
