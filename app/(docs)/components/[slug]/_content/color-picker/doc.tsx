import type { DocModule } from "@/app/(docs)/components/schema";

export default {
  sections: [
    {
      demo: {
        group: "color-picker",
        name: "main",
      },
    },
  ],
} satisfies DocModule;
