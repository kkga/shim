import type { DocModule } from "@/app/(docs)/components/schema";

export default {
  sections: [
    {
      demo: {
        group: "calendar",
        name: "main",
      },
    },
  ],
} satisfies DocModule;
