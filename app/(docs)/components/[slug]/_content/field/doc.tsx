import type { DocModule } from "@/app/(docs)/components/schema";

export default {
  sections: [
    {
      demo: {
        group: "field",
        name: "main",
      },
    },
  ],
} satisfies DocModule;
