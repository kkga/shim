
<Demo title="Variant" demo={<TabsDemo.Variant />} code={demos.variant}>
  Use the `variant` prop on the `TabList` component to change the appearance of
  the tabs.
</Demo>

<Demo title="Size" demo={<TabsDemo.Size />} code={demos.size}>
  Use the `size` prop on the `TabList` component to change the size of the tabs.
</Demo>

<Demo
  title="Orientation"
  demo={<TabsDemo.Orientation />}
  code={demos.orientation}
>
  Use the `orientation` prop to change the orientation of the tabs.
</Demo>

<Demo
  title="Controlled "
  demo={<TabsDemo.Controlled />}
  code={demos.controlled}
>
  Use the `selectedKey` and `onSelectionChange` props to control the selected
  tab.
</Demo>

<Demo
  title="Dynamic content"
  demo={<TabsDemo.Dynamic />}
  code={demos.dynamic}
/>
