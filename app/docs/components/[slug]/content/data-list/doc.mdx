
<Demo
  title="Orientation"
  demo={<DataListDemo.LabelPosition />}
  code={demos["label-position"]}
>
  Use `labelPosition` to set the position of the labels.
</Demo>

<Demo title="Size" demo={<DataListDemo.Size />} code={demos.size}>
  Use `size` to set the size of the DataList. Child components that support the
  `size` prop will inherit it.
</Demo>
