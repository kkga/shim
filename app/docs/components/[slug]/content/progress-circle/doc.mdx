
### Size

Use `size` prop to change the size of the progress circle.

<Demo demo={<ProgressCircleDemo.Size />} code={demos.size} />

### Indeterminate

Use `isIndeterminate` prop to display an indeterminate progress bar.

<Demo demo={<ProgressCircleDemo.Indeterminate />} code={demos.indeterminate} />
