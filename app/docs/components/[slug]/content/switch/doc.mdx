
<Demo title="States" demo={<SwitchDemo.States />} code={demos.states}>
  Use `defaultSelected` prop to set the initial state of the switch. Use
  `isDisabled` prop to disable the switch.
</Demo>

<Demo title="Variant" demo={<SwitchDemo.Variant />} code={demos.variant}>
  Use `variant` prop to set the appearance of the switch.
</Demo>

<Demo
  title="Label position"
  className="items-start"
  demo={<SwitchDemo.LabelPosition />}
  code={demos["label-position"]}
>
  Use `labelPosition` prop to set the position of the label.
</Demo>

<Demo title="Size" demo={<SwitchDemo.Size />} code={demos.size}>
  Use `size` prop to set the size of the switch.
</Demo>

<Demo
  title="Controlled"
  demo={<SwitchDemo.Controlled />}
  code={demos.controlled}
>
  Use `isSelected` and `onChange` props to control the state of the switch.
</Demo>
