---
title: Get started
description: Setup the components in your project.
order: 1
---

## What is Shim?

Copy-paste UI components built on top of [React Aria Components](https://react-spectrum.adobe.com/react-aria/index.html). You own the code.

## Install the prerequisites

<Steps>
  <Step>
    ### React Aria Components

    Core accessible primitives used by Shim components. Read the docs at [react-spectrum.adobe.com/react-aria/components](https://react-spectrum.adobe.com/react-aria/components.html).

    <CodeBlock code={[ { title: "Terminal", content: "pnpm add react-aria-components", }, ]} />

  </Step>

  <Step>
    ### Tailwind CSS v4

    Utility-first styling foundation. Installation depends on your framework—follow the [official Tailwind guide](https://tailwindcss.com/docs) for the correct steps.

  </Step>

  <Step>
    ### tailwind-variants

    Declarative variant helpers for Tailwind class strings. See the docs at [tailwind-variants.org](https://www.tailwind-variants.org/).

    <CodeBlock
      code={[
        {
        title: "Install",
        content: "pnpm add tailwind-variants",
        },
      ]}
    />

  </Step>

  <Step>
    ### tailwind-merge

    Safely merge conditional Tailwind classes. Documentation lives at [github.com/dcastil/tailwind-merge](https://github.com/dcastil/tailwind-merge).

    <CodeBlock
      code={[
        {
        title: "Install",
        content: "pnpm add tailwind-merge",
        },
      ]}
    />

  </Step>

  <Step>
    ### Radix Colors
    Semantic color palette used by Shim tokens. Browse palettes at [radix-ui.com/colors](https://www.radix-ui.com/colors).
    <CodeBlock
      code={[
        {
        title: "Install",
        content: "pnpm add @radix-ui/colors",
        },
      ]}
    />

  </Step>

  <Step>
    ### Phosphor Icons
    Icon set referenced by several components. Explore the catalog at [phosphoricons.com](https://phosphoricons.com/).
    <CodeBlock code={[{ title: "Install", content: "pnpm add @phosphor-icons/react", }]} />

  </Step>
</Steps>

## Shim setup

<Steps>
  <Step>
    ### Configure path alias

    Shim components import files via the `@/` alias. Configure the path in `tsconfig.json` (or `jsconfig.json`) so those imports resolve:

    <CodeBlock
      code={[
        {
          title: "tsconfig.json",
          content: `{
    "compilerOptions": {
      "baseUrl": ".",
      "paths": {
        "@/*": ["./*"]
      }
    }
}`,
        },
      ]}
    />

  </Step>
  <Step>
    ### Initialize Shim

    Run the `init` command to initialize Shim. 
    This command:

    - Creates `shim.config.json` with your chosen paths
    - Downloads the required utility files (`style.ts`, `theme.tsx`)
    - Fetches the theme CSS file (`theme.css`)


    <CodeBlock code={[{ title: "Terminal", content: "pnpm dlx @kkga/shim init", }]} />

  </Step>
</Steps>


You can customise the paths during setup:

```bash
pnpm dlx @kkga/shim init --components-path src/ui --utils-path src/lib --css-path src/styles
```

Sample configuration:

<CodeBlock
  code={[
    {
      title: "shim.config.json",
      content: `{
"componentsPath": "src/ui",
"utilsPath": "src/lib",
"cssPath": "src/styles"
}`,
    },
  ]}
/>

## Add components

### Using the CLI

```bash
pnpm dlx @kkga/shim add button dialog
```

The CLI resolves dependencies, rewrites imports, and installs into the paths defined in `shim.config.json`.

### Manual copy-paste

1. Browse the component source in the [Shim repository](https://github.com/kkga/shim/tree/master/shim-ui)
2. Copy the component `.tsx` file into your project
3. Copy required utility files (`lib/style.ts`, `lib/theme.tsx`) and `css/theme.css`
4. Update import paths (or set up a path alias) so component imports continue to resolve. Aliases keep your project structure flexible and match what the CLI generates.
