---
name: DataList
description: Display key-value pairs in a list.
category: Content
---

<Demo
  title="Orientation"
  demo={<DataListDemo.LabelPosition />}
  code={demos["label-position"]}
>
  Use the `labelPosition` prop to set the position of the labels.
</Demo>

<Demo title="Size" demo={<DataListDemo.Size />} code={demos.size}>
  Use the `size` prop to set the size of the DataList. Child components that
  support the `size` prop will inherit the size from the parent DataList.
</Demo>
