---
name: DataList
description: Display key-value pairs in a list.
category: Content
---

### Orientation

Use the `labelPosition` prop to set the position of the labels.

<Demo demo={<DataListDemo.LabelPosition />} code={demos["label-position"]} />

### Size

Use the `size` prop to set the size of the DataList.

<Demo stacked demo={<DataListDemo.Size />} code={demos.size} />
