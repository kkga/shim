---
name: ComboBox
description: Filter a list of options by typing in a text field.
category: "Pickers"
docUrl: https://react-spectrum.adobe.com/react-aria/ComboBox.html
ariaUrl: https://www.w3.org/WAI/ARIA/apg/patterns/combobox/
composes:
  - Field
  - ListBox
  - Popover
---

<Demo title="Size" demo={<ComboBoxDemo.Size />} code={demos.size}>
  Use `size` to set the size of the ComboBox.
</Demo>

<Demo title="Content" demo={<ComboBoxDemo.Content />} code={demos.content} >
ComboBox follows the
[Collections API](https://react-spectrum.adobe.com/react-aria/collections.html)
and accepts both static and dynamic items.

To use dynamic items, pass an `items` prop to the ComboBox component and a
render function as children. The render function will be called with each item
in the `items` iterable and should return a React element.

</Demo>
