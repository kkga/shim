---
name: RadioGroup
description: Mark one option as selected.
category: 'Forms'
srcFilename: 'radio'
docUrl: 'https://react-spectrum.adobe.com/react-aria/RadioGroup.html'
composes: ['Field']
aria: 'radio'
---

## Usage

Import `Radio` and `RadioGroup` components to compose a group of radio buttons.
Use `label` and `description` props to provide context to the group.

<Demo demo={<RadioGroupDemo.Main />} code={demos['main']} />

---

## Examples

### Orientation

Use `orientation` prop to change the layout of the radio buttons.

<Demo demo={<RadioGroupDemo.Orientation />} code={demos['orientation']} />
