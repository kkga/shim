---
name: ToggleButtonGroup
description: Group multiple toggle buttons, with single or multiple selection.
category: Buttons
docUrl: https://react-spectrum.adobe.com/react-aria/ToggleButtonGroup.html
ariaUrl: https://www.w3.org/WAI/ARIA/apg/patterns/toolbar/
composes:
  - Field
  - ToggleButton
---

<Demo
  title="Selection mode"
  demo={<ToggleButtonGroupDemo.SelectionMode />}
  code={demos["selection-mode"]}
  className="items-start"
>
  Use `selectionMode` to set the selection mode of the button group.
</Demo>

<Demo
  title="States"
  demo={<ToggleButtonGroupDemo.States />}
  code={demos["states"]}
  className="items-start"
>
  Use `isDisabled` to disable the entire group or individual buttons.
</Demo>

<Demo
  title="Size"
  demo={<ToggleButtonGroupDemo.Size />}
  code={demos["size"]}
  className="self-start"
>
  Use `size` prop to set the size the button group.
</Demo>

<Demo
  title="Variant"
  demo={<ToggleButtonGroupDemo.Variant />}
  code={demos["variant"]}
  className="self-start"
>
  Use `variant` prop to set the visual appearance of the button group.
</Demo>
