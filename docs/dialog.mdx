---
name: Dialog
description: Modal overlay displayed above other content.
category: 'Overlays'
srcFilename: 'dialog'
docUrl: https://react-spectrum.adobe.com/react-aria/Modal.html
aria: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/
---

### Accessing uncontrolled state

If you need to render a close button in an uncontrolled Dialog, wrap the
children into a function that receives a `close` function as a parameter. This
function can be called to close the Dialog.

<Demo demo={<DialogDemo.Close />} code={demos.close} />
