---
name: SearchField
description: Enter and clear a search query.
category: 'Forms'
srcFilename: 'search-field'
docUrl: 'https://react-spectrum.adobe.com/react-aria/SearchField.html'
aria: https://www.w3.org/TR/wai-aria-1.2/#searchbox
composes: ['Field']
---

### Controlled value

Use the _value_ and _onChange_ props to control the value of the search field.

<Demo
  className="items-stretch"
  demo={<SearchFieldDemo.Controlled />}
  code={demos['controlled']}
/>

### Size

Use the _size_ prop to set the size of the search field.

<Demo
  className="items-stretch"
  demo={<SearchFieldDemo.Size />}
  code={demos['size']}
/>

### Prefix icon

Use the _prefixIcon_ prop to set a prefix icon for the search field. Set to
_"filter"_ when using the field as a filter input or pass a custom icon
component.

<Demo
  className="items-stretch"
  demo={<SearchFieldDemo.Icon />}
  code={demos['icon']}
/>

### States

Use _isDisabled_, _isRequired_, _isInvalid_ and _errorMessage_ props to set the
state of the search field.

<Demo
  className="items-stretch"
  demo={<SearchFieldDemo.States />}
  code={demos['states']}
/>
