---
name: Field
description:
  Utility components for other form fields. Not intended to be used directly.
category: "Forms"
---

### Field context

Use `FieldContext` provider to pass down default `size`, `variant` and
`labelPosition` to all form field components. This is useful when you want to
set default values for all form fields in a form.

<Demo stacked demo={<FieldDemo.Context />} code={demos["field-context"]} />

<Demo
  stacked
  demo={<FieldDemo.ContextSize />}
  code={demos["field-context-size"]}
/>
