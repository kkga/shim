---
name: SearchField
description: Enter and clear a search query.
category: Forms
docUrl: https://react-spectrum.adobe.com/react-aria/SearchField.html
aria: https://www.w3.org/TR/wai-aria-1.2/#searchbox
composes:
  - Field
---

### Controlled value

Use `value` and `onChange` to control the value of the search field.

<Demo
  className="items-stretch"
  demo={<SearchFieldDemo.Controlled />}
  code={demos["controlled"]}
/>

### Size

Use `size` to set the size of the search field.

<Demo
  className="items-stretch"
  demo={<SearchFieldDemo.Size />}
  code={demos["size"]}
/>

### Variant

Use `variant` to set the variant of the search field.

<Demo
  className="items-stretch"
  demo={<SearchFieldDemo.Variant />}
  code={demos["variant"]}
/>

### Prefix icon

Use `prefixIcon` to set the prefix icon for the search field. Set to `"filter"`
when using the field as a filter input or pass a custom icon component.

<Demo
  className="items-stretch"
  demo={<SearchFieldDemo.PrefixIcon />}
  code={demos["prefix-icon"]}
/>

### States

Use `isDisabled`, `isRequired`, `isInvalid` and `errorMessage` to set the state
of the search field.

<Demo
  className="items-stretch"
  demo={<SearchFieldDemo.States />}
  code={demos["states"]}
/>
