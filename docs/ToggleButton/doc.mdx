---
name: ToggleButton
description: Toggle a state between two values.
category: Buttons
docUrl: https://react-spectrum.adobe.com/react-aria/ToggleButton.html
ariaUrl: https://www.w3.org/WAI/ARIA/apg/patterns/button/
---

<Demo
  title="Size"
  className="flex-row items-start"
  demo={<ToggleButtonDemo.Size />}
  code={demos["size"]}
>
  Use `size` prop to set the button size.
</Demo>

<Demo
  title="Intent"
  className="flex-row items-start"
  demo={<ToggleButtonDemo.Intent />}
  code={demos["intent"]}
>
  Use `intent` prop to set the semantic color.
</Demo>

<Demo
  title="Variant"
  className="flex-row items-start"
  demo={<ToggleButtonDemo.Variant />}
  code={demos["variant"]}
>
  Use `variant` prop to set the visual style.
</Demo>

<Demo
  title="States"
  className="flex-row items-start"
  demo={<ToggleButtonDemo.States />}
  code={demos["states"]}
>
  Use `isSelected` and `isDisabled` props to control the button state.
</Demo>

<Demo
  title="Controlled"
  className="items-start"
  demo={<ToggleButtonDemo.Controlled />}
  code={demos["controlled"]}
>
  Use `isSelected` and `onChange` props to control the state.
</Demo>
