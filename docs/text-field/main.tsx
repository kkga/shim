import { TextField } from "@/components/text-field";

export default () => (
  <TextField
    description="Visible to others"
    label="First name"
    labelPosition="side"
    placeholder="John"
  />
);
