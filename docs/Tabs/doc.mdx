---
name: Tabs
description: Navigate between different sections of content.
category: Navigation
docUrl: "https://react-spectrum.adobe.com/react-aria/Tabs.html"
aria: tabs
---

### Variant

Use the `variant` prop on the `TabList` component to change the appearance of
the tabs.

<Demo demo={<TabsDemo.Variant />} code={demos.variant} />

### Size

Use the `size` prop on the `TabList` component to change the size of the tabs.

<Demo demo={<TabsDemo.Size />} code={demos.size} />

### Orientation

Use the `orientation` prop to change the orientation of the tabs.

<Demo demo={<TabsDemo.Orientation />} code={demos.orientation} />

### Controlled selection

Use the `selectedKey` and `onSelectionChange` props to control the selected tab.

<Demo demo={<TabsDemo.Controlled />} code={demos.controlled} />

## Content

<Demo demo={<TabsDemo.Dynamic />} code={demos.dynamic} />
