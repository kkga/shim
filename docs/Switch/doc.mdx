---
name: Switch
description: Toggle between two states.
category: "Forms"
docUrl: "https://react-spectrum.adobe.com/react-aria/Switch.html"
aria: "https://www.w3.org/WAI/ARIA/apg/patterns/switch/"
---

### States

Use `defaultSelected` to set the initial state of the switch. Use `isDisabled`
to disable the switch.

<Demo demo={<SwitchDemo.States />} code={demos.states} />

### Variant

Use `variant` to set the appearance of the switch.

<Demo demo={<SwitchDemo.Variant />} code={demos.variant} />

### Size

Use `size` to set the size of the switch.

<Demo demo={<SwitchDemo.Size />} code={demos.size} />

### Controlled

Use `isSelected` and `onChange` to control the state of the switch.

<Demo demo={<SwitchDemo.Controlled />} code={demos.controlled} />
