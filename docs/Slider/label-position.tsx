import { Slider } from "@ui/Slider"

export default () => (
  <>
    <Slider labelPosition="top" defaultValue={37} label="Slope" />
    <Slider labelPosition="side" defaultValue={37} label="Slope" />
  </>
)
